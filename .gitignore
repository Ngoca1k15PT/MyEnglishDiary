# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Course media is ingested locally from your own files (hundreds of MB).
# The manifest in src/data/courses/ is committed; the media is not.
public/course
# Staging clone that `firebase deploy` reads from (must live next to firebase.json).
scripts/course-cdn/media
.firebase/
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
